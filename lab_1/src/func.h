#pragma once //Указывает, что компилятор включает файл заголовка только один раз при компиляции файла исходного кода.
#include <iostream>
#include <math.h>

using namespace std;

pair<int, int> func(int a);